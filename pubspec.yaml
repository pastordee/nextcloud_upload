name: nextcloud_upload
version: 1.0.0
description: A minimal Nextcloud client for file upload and share creation.

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  http: "^1.5.0"
  xml: "^6.6.0"

dev_dependencies:
  test: "^1.26.3"
